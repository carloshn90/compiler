class Server {

    var serverId;

    fun Server(id) {
        serverId = id;
    }

    fun start() {
        return "Starting server number: " + serverId;
    }

    fun oddOrEven(until) {

        for (var i = 0; i < until; i = 1 + i) {
            if ((i % 2) == 0) {
                print i + " is an even number";
            } else {
                print i + " is an odd number";
            }
        }
    }
}

var server = Server(1.0);
print server.start();
server.oddOrEven(6);
